# ЗОЖ-трекер «Пью воду, сплю» 💧

Курсовой проект по дисциплине «Веб-технологии»
**Вариант 21**

## Общая идея проекта

Приложение для отслеживания ключевых показателей здорового образа жизни (ЗОЖ).
Пользователь самостоятельно определяет, какие метрики он хочет отслеживать (потребление воды, сон, шаги, медитация, вес и т.д.), ставит цели и фиксирует ежедневные значения. Приложение показывает прогресс, графики и напоминания.

**Питч:** здоровье — это привычка.

## Технологический стек

- **Backend:** Python + Flask
- **ORM:** Flask-SQLAlchemy
- **Аутентификация:** JWT (Flask-JWT-Extended) или Flask-Login
- **База данных:** SQLite (для разработки) / PostgreSQL (для продакшена)
- **Миграции:** Flask-Migrate (Alembic)
- **Frontend:** на выбор студента (React/Vue/Svelte, либо чистый HTML + JS + Bootstrap/Tailwind)
- **Графики:** Chart.js (или аналог)

## MVP — обязательные требования

### Пользователи и аутентификация

- Регистрация и вход (email + пароль).
- Защита всех API-эндпоинтов (только авторизованные пользователи работают со своими данными).
- Один пользователь — один аккаунт (роли не требуются).

### Основные сущности

- **Metric** (метрика)
  - name, unit, target_value (опционально), color (опционально)

- **Goal** (цель по метрике, опционально)
  - target_value, period (daily/weekly/monthly), start_date, end_date

- **Entry** (запись значения)
  - metric_id, value, date, note (опционально)

### API (RESTful, префикс `/api/v1`)

| Метод  | Эндпоинт                          | Описание                                           |
|--------|-----------------------------------|----------------------------------------------------|
| POST   | /auth/register                    | Регистрация                                        |
| POST   | /auth/login                       | Вход (возвращает JWT-токен)                        |
| GET    | /metrics                          | Список метрик пользователя                         |
| POST   | /metrics                          | Создание метрики                                   |
| GET    | /metrics/<id>                     | Детали метрики                                     |
| PUT    | /metrics/<id>                     | Обновление метрики                                 |
| DELETE | /metrics/<id>                     | Удаление метрики (каскадно удаляет записи)         |
| GET    | /metrics/<id>/entries             | Записи по метрике (?date_from, ?date_to)           |
| POST   | /entries                          | Создание записи                                    |
| PUT    | /entries/<id>                     | Редактирование записи                              |
| DELETE | /entries/<id>                     | Удаление записи                                    |
| GET    | /dashboard                        | Сводка за сегодня/неделю                           |
| GET    | /reports                          | Данные для графиков (по метрике и диапазону дат)   |

- Валидация данных и понятные сообщения об ошибках.
- Корректные HTTP-коды.

### Frontend (клиентская часть)

- Страницы входа/регистрации.
- Дашборд: список метрик с прогрессом за сегодня, кнопка добавления метрики и быстрых записей.
- Страница метрики: история записей, форма добавления/редактирования, график (Chart.js).
- Адаптивный дизайн, базовая доступность (a11y).

### База данных

- Миграции через Flask-Migrate.
- Индексы по `user_id`, `metric_id`, `date`.
- Сидинг: после регистрации создать примерные метрики («Вода», «Сон», «Шаги»).

### Нефункциональные требования

- CORS только для своего фронтенда.
- Базовая безопасность (Flask-Talisman или ручные заголовки).
- Логирование запросов и ошибок.

## Критерии приёмки MVP

Демонстрация должна показать:

1. Регистрацию и вход.
2. Создание собственных метрик с целями.
3. Добавление записей за разные даты.
4. Актуальный дашборд с прогрессом за сегодня.
5. Историю и графики по метрикам.
6. Работу через API (проверка в Postman/Swagger).

## Бонусные улучшения (по желанию)

- Напоминания (Web Push или email через Celery + Redis).
- Заглушка интеграции с wearables.
- PWA (офлайн-доступ).
- Dark mode.
- Расширенные отчёты и streaks.
- Документация API (Swagger/OpenAPI).
- Тесты (unit/integration/e2e).
- Деплой в Kubernetes.
- CI/CD (GitHub Actions).
- WebSocket для реального времени.
